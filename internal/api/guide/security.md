# 安全边界

- 密码使用 bcrypt 哈希，不使用 MD5。
- 登录和无效 Agent Token 连续 5 次失败会临时封禁来源 IP，并返回可操作提示。
- SSH 凭据加密保存，API 不会回传密码或私钥。
- 官方指南接口只返回本页面中的静态说明；不会返回任何账户、Token、SSH、服务器、项目、日志或部署状态。
- 日志保留 30 天；Agent 仅允许上报受限错误码，避免把敏感输出写进控制面。
